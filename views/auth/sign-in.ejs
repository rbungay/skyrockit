<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %><br />

    <h1>Sign in</h1>
    <form action="/auth/sign-in" method="POST" class="signup-form">
      <!-- Using the same class as sign-up -->
      <label for="username">Username:</label>
      <input
        type="text"
        name="username"
        id="username"
        class="form-input"
        required
      />
      <!-- Added class for styling -->
      <label for="password">Password:</label>
      <input
        type="password"
        name="password"
        id="password"
        class="form-input"
        required
      />
      <!-- Added class for styling -->
      <button type="submit" class="form-button">Sign in</button>
      <!-- Added class for styling -->
    </form>
  </body>
</html>
